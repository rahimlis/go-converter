input:
    input.go: |
        package structs

        // goverter:converter
        // goverter:extend HelloWorld
        type Converter interface {
        }
        func HelloWorld() int {
            return 0
        }
error: |-
    Error while parsing extend in
        github.com/rahimlis/go-converter/execution.Converter

    HelloWorld has no or too many parameters
