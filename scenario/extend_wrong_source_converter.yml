input:
    input.go: |
        package structs

        // goverter:converter
        // goverter:extend HelloWorld
        type Converter interface {
        }
        func HelloWorld(int, int) int {
            return 0
        }
error: |-
    Error while parsing extend in
        github.com/rahimlis/go-converter/execution.Converter

    the first parameter must be of type github.com/rahimlis/go-converter/execution.Converter
